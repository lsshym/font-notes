第一版本

```javascript
function test(fn, wait) {
    var timeOut = null
    return function () {
        console.log('tt')
        // 一直执行，timeout一直在被赋值，一直被清空，直到最后一次
        if (timeOut !== null) {
            clearTimeout(timeOut)
        }
         timeOut = setTimeout(fn, wait);
    }
}
```

